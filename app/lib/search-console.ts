import 'server-only'

export const searchConsoleNotes = {
  legacyStorePath:
    'Despues del deploy, pedir reindexacion de /productos y revisar la eliminacion temporal de /tienda en Google Search Console si el resultado viejo sigue apareciendo.',
} as const
